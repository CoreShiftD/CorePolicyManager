# CoreShift Policy v0.1.0-preview.2

Preview/testing release.

Changes:
- removed legacy `-p` flag
- added clean `-f` / `--feature` CLI flags
- no-arg command now shows help
- improved status/runtime hardening
- bundled JSON assets in Magisk package
- async app index worker
- foreground-aware preload/runtime
