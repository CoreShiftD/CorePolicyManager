#!/system/bin/sh

# Prefer leaving logs for now.
# Only remove the working directory if safe, but we'll just leave it and print where logs remain.
echo "CoreShift Policy logs and state remain in /data/local/tmp/coreshift"
